
describe 'jQuery-Smoothie Plugin'
  describe '.smoothie()'
		it 'should be a function'
			jQuery.isFunction(jQuery().smoothie).should.be true
		end
  end

	describe 'smooth scrolling'
		before_each
		end
	end
end